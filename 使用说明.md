# Table Tool - Cocos Creator 跨版本 Vue 数据编辑器

## 📖 项目简介

**Table Tool** 是一个基于 **Cocos Creator 跨版本插件框架** 开发的 **Vue 数据编辑器工具**，同时兼容 **Cocos Creator 2.x** 和 **3.x** 版本，采用适配器模式实现跨版本 API 统一。

### 🎯 项目目标

打造一个功能强大的表格数据编辑器，用于在 Cocos Creator 编辑器中便捷地管理和编辑游戏配置数据。

### 核心特性

- ✅ **跨版本兼容**：自动检测 Cocos Creator 版本，使用对应的 API
- ✅ **统一接口**：提供一致的开发体验，无需编写版本判断代码
- ✅ **模块化设计**：核心功能与业务逻辑分离，易于扩展维护
- ✅ **TypeScript 支持**：完整的类型定义，开发更安全高效
- ✅ **Vue 面板**：基于 Vue 框架的现代化数据编辑面板（开发中）

---

## 🚀 快速开始

### 1. 安装插件

将插件文件夹复制到项目的 `packages` 目录下：

```
your-project/
└── packages/
    └── table_tool/
```

### 2. 编译插件

在插件目录下运行：

```bash
npm install
npm run build
```

或分别编译 v2/v3 版本：

```bash
npm run build:v2  # 编译 Cocos 2.x 版本
npm run build:v3  # 编译 Cocos 3.x 版本
```

### 3. 启用插件

**Cocos Creator 2.x:**
- 顶部菜单：`扩展 → 扩展管理器`
- 找到 `table_tool` 并启用

**Cocos Creator 3.x:**
- 顶部菜单：`扩展 → Extension Manager`
- 找到 `table_tool` 并启用

### 4. 重载插件（可选）

**Cocos Creator 2.x:**
- `开发者 → 重新加载插件`

**Cocos Creator 3.x:**
- `开发者 → 重新加载扩展`

---

## 🔧 功能说明

### 1. 数据编辑器面板（开发中）

#### 打开方式

**Cocos Creator 2.x:**
- 菜单：`扩展 → Table Tool → Open Panel`

**Cocos Creator 3.x:**
- 菜单：`面板 → Table Tool → Open Panel`

#### 计划功能

- 🔄 Vue 数据编辑界面
- 🔄 表格数据编辑与管理
- 🔄 数据导入/导出（Excel/JSON/CSV）
- 🔄 数据验证与校验

### 2. Bundle 路径映射工具

自动扫描项目中的资源 Bundle，生成路径映射配置。

#### 使用方式

**Cocos Creator 2.x:**
- 菜单：`扩展 → Table Tool → 映射资源名称路径`

**Cocos Creator 3.x:**
- 菜单：`工具 → Table Tool → 映射资源名称路径`

#### 功能说明

- 自动扫描 `assets` 目录下的所有 Bundle
- 生成资源路径映射关系
- 便于在代码中引用资源路径

------

## 📁 项目结构

```
cc_table_tool/
├── package.json              # 主配置文件（Cocos 3.x 格式）
├── package.v2.json           # Cocos 2.x 专用配置
├── package.v3.json           # Cocos 3.x 专用配置（含 asset-db.mount）
├── tsconfig.json             # TypeScript 基础配置
├── tsconfig.v2.json          # v2 编译配置
├── tsconfig.v3.json          # v3 编译配置
├── README.md                 # 项目说明（简要）
├── 使用说明.md              # 本文档（详细）
│
├── assets/                   # 游戏运行时工具类（V3 自动挂载，V2 双向同步）
│
├── src/                      # 编辑器插件源代码
│   ├── index.ts              # 统一导出入口
│   ├── main.ts               # 插件主入口
│   │
│   ├── core/                 # 核心框架
│   │   ├── interfaces.ts     # 跨版本抽象接口定义
│   │   ├── version-detector.ts # 版本检测器
│   │   ├── factory.ts        # 适配器工厂
│   │   └── base-plugin.ts    # 基础插件类
│   │
│   ├── adapters/             # 版本适配器
│   │   ├── v2/               # Cocos 2.x 适配器
│   │   │   ├── editor-adapter.ts
│   │   │   ├── panel-adapter.ts
│   │   │   ├── ipc-adapter.ts
│   │   │   ├── asset-adapter.ts
│   │   │   └── scene-adapter.ts
│   │   │
│   │   └── v3/               # Cocos 3.x 适配器
│   │       ├── editor-adapter.ts
│   │       ├── panel-adapter.ts
│   │       ├── ipc-adapter.ts
│   │       ├── asset-adapter.ts
│   │       └── scene-adapter.ts
│   │
│   ├── tools/                # 编辑器工具类
│   │   └── Tools.ts          # 文件操作、双向同步等
│   │
│   ├── business/             # 业务功能模块
│   │   └── my-panel.ts       # 面板实现示例
│   │
│   └── panels/               # 面板定义
│
├── dist/                     # 编译输出目录
│   ├── v2/                   # v2 编译产物
│   └── v3/                   # v3 编译产物
│
├── i18n/                     # 国际化
│   ├── zh.js                 # 中文
│   └── en.js                 # 英文
│
└── scripts/                  # 构建脚本
    └── install.js            # 安装脚本

```

---

## 🛠️ 开发指南

### 核心设计模式

#### 1. 抽象接口层（interfaces.ts）

定义所有跨版本通用的接口，屏蔽版本差异：

```typescript
export interface IEditorAdapter {
    getVersion(): string;
    log(message: string, ...args: any[]): void;
    getProjectPath(): string;
}

export interface IPanelAdapter {
    open(panelName: string): void;
    close(panelName: string): void;
}

export interface IIPCAdapter {
    sendToMain(channel: string, ...args: any[]): void;
    sendToPanel(panelName: string, channel: string, ...args: any[]): void;
}
```

#### 2. 版本检测（version-detector.ts）

自动检测当前运行的 Cocos Creator 版本：

```typescript
export class VersionDetector {
    static detect(): CocosVersion {
        // 自动检测逻辑
    }
    
    static isV2(): boolean { /* ... */ }
    static isV3(): boolean { /* ... */ }
}
```

#### 3. 适配器工厂（factory.ts）

根据版本动态创建对应的适配器实例：

```typescript
export class AdapterFactory {
    static getInstance(): IAdapterManager {
        const version = VersionDetector.detect();
        // 返回对应版本的适配器
    }
}
```

#### 4. 基础插件类（base-plugin.ts）

提供统一的插件开发基类：

```typescript
export abstract class BasePlugin {
    protected get editor(): IEditorAdapter { /* ... */ }
    protected get panel(): IPanelAdapter { /* ... */ }
    protected get ipc(): IIPCAdapter { /* ... */ }
    
    abstract load(): void;
    abstract unload(): void;
}
```

### 添加新功能

#### 1. 创建业务模块

在 `src/business/` 下创建新文件：

```typescript
// src/business/MyFeature.ts
import { getEditor, getPanel } from '../index';

export class MyFeature {
    static run() {
        const editor = getEditor();
        editor.log('My feature is running!');
        
        // 使用统一接口，无需关心版本
        const panel = getPanel();
        panel.open('my-panel');
    }
}
```

#### 2. 在主插件中注册

在 `src/main.ts` 中添加消息处理：

```typescript
class ExtensionsToolsPlugin extends BasePlugin {
    messages = {
        'my-feature': () => this.handleMyFeature(),
    };
    
    methods = {
        myFeature: () => this.handleMyFeature(),
    };
    
    private handleMyFeature() {
        MyFeature.run();
    }
}
```

#### 3. 配置菜单项

**package.json (v2):**
```json
{
    "main-menu": {
        "i18n:MAIN_MENU.package.title/extensions_tools/my-feature": {
            "message": "extensions_tools:my-feature"
        }
    }
}
```

**package.v3.json (v3):**
```json
{
    "contributions": {
        "menu": [{
            "path": "i18n:menu.develop/My Feature",
            "message": "myFeature"
        }],
        "messages": {
            "myFeature": {
                "methods": ["myFeature"]
            }
        }
    }
}
```

### 创建自定义面板

参考 `src/business/my-panel.ts` 和 `src/panels/default/index.ts` 的实现方式。

---

## 🎯 使用示例

### 示例 1：获取编辑器信息

```typescript
import { getEditor, isV2, isV3 } from './index';

const editor = getEditor();
console.log('版本:', editor.getVersion());
console.log('项目路径:', editor.getProjectPath());
console.log('是否 v2:', isV2());
console.log('是否 v3:', isV3());
```

### 示例 2：打开面板

```typescript
import { getPanel } from './index';

const panel = getPanel();
panel.open('table_tool');
```

### 示例 3：发送消息

```typescript
import { getIPC } from './index';

const ipc = getIPC();
ipc.sendToPanel('table_tool', 'hello', { data: 'test' });
ipc.broadcast('global-event', { message: 'hello world' });
```

### 示例 4：操作资源

```typescript
import { getAsset } from './index';

const asset = getAsset();
const uuid = asset.urlToUuid('db://assets/scripts/Main.ts');
console.log('资源 UUID:', uuid);
```

---

## ⚙️ 配置说明

### package.json 配置项

| 字段 | v2 | v3 | 说明 |
|------|----|----|------|
| 主入口 | `main` | `main` | 插件主文件路径 |
| 菜单 | `main-menu` | `contributions.menu` | 顶部菜单配置 |
| 面板 | `panel` | `panels` | 面板配置 |
| 消息 | - | `contributions.messages` | 消息映射 |
| Inspector | `inspector` | `inspector` | 检查器扩展 |

### TypeScript 配置

- `tsconfig.json` - 基础配置
- `tsconfig.v2.json` - v2 编译配置（输出到 `dist/v2`）
- `tsconfig.v3.json` - v3 编译配置（输出到 `dist/v3`）

---

## 🐛 常见问题

### Q1: 插件没有生效？

**解决方法：**
1. 检查插件是否已启用
2. 尝试重新加载插件/扩展
3. 检查是否已执行编译命令
4. 查看控制台是否有错误信息

### Q2: Inspector 没有显示？

**解决方法：**
1. 确认已选中**文件夹**（不是文件）
2. 检查文件夹内是否有正确命名的 `.{文件夹名}.md` 文件
3. 重新编译插件
4. 重启 Cocos Creator

### Q3: 跨版本开发注意事项？

**建议：**
- 始终使用适配器提供的接口，不要直接调用 `Editor` 对象
- 面板开发时注意 v2 和 v3 的 DOM API 差异
- 测试时在两个版本中都验证功能

### Q4: 如何调试？

**Cocos Creator 2.x:**
- `开发者 → 开发者工具` 打开控制台

**Cocos Creator 3.x:**
- `开发者 → 开发者工具` 打开控制台

在代码中使用：
```typescript
this.editor.log('调试信息');
console.log('更多调试信息');
```

---

## 🔨 编辑器工具类 (Tools)

本插件提供了一套编辑器专用的工具类，位于 `src/tools/Tools.ts`，包含文件操作、目录同步等功能。

> **注意**：此工具类仅在编辑器环境中使用，不会同步到项目中。

### 工具方法概览

| 方法 | 说明 |
|------|------|
| `CopyFileSync` | 同步复制文件 |
| `CopyDirSync` | 同步复制目录 |
| `WriteFileSync` | 同步写入文件（自动创建目录） |
| `ReadDir` | 遍历目录 |
| `OpenInterfaceSelectFolder` | 弹出文件夹选择对话框 |
| `OpenInterfaceSelectFile` | 弹出文件选择对话框 |
| `GetDirectoryModifyTime` | 获取目录最新修改时间 |
| `DeleteFilesByPattern` | 按模式删除文件 |
| `BidirectionalSync` | 双向目录同步 |

### 文件操作示例

```typescript
import { Tools } from '../tools/Tools';

// 复制文件
Tools.CopyFileSync(srcFile, destFile);

// 复制目录
Tools.CopyDirSync(srcDir, destDir, true); // 第三个参数开启日志

// 写入文件（自动创建目录）
Tools.WriteFileSync(filePath, content, { encoding: 'utf8' });

// 遍历目录
Tools.ReadDir(dirPath, (filePath, fileName) => {
    console.log(`Found: ${fileName} at ${filePath}`);
});
```

### 文件选择对话框

```typescript
import { Tools } from '../tools/Tools';

// 选择文件夹
const folderPath = await Tools.OpenInterfaceSelectFolder(defaultPath);

// 选择文件（可指定扩展名）
const jsonFile = await Tools.OpenInterfaceSelectFile('json', defaultPath);
const xlsxFile = await Tools.OpenInterfaceSelectFile('xlsx');
const anyFile = await Tools.OpenInterfaceSelectFile();
```

### 双向同步使用

```typescript
import { Tools, IBidirectionalSyncOptions } from '../tools/Tools';

const result = Tools.BidirectionalSync(dirA, dirB, {
    ignorePatterns: ['.meta', '.git'],  // 计算时间时忽略的文件
    deletePatterns: ['.meta'],          // 同步后删除的文件
    preferDir: 'auto',                  // 'auto' | 'A' | 'B'
    onLog: console.log,
    onWarn: console.warn,
    onError: console.error
});

if (result.synced) {
    console.log(`同步方向: ${result.direction}`);
    console.log(`源: ${result.sourceDir}`);
    console.log(`目标: ${result.targetDir}`);
}
```

---

## 📚 API 参考

### BasePlugin 基类

| 方法/属性 | 说明 |
|----------|------|
| `load()` | 插件加载时调用（需重写） |
| `unload()` | 插件卸载时调用（需重写） |
| `editor` | 编辑器适配器 |
| `panel` | 面板适配器 |
| `ipc` | 进程通信适配器 |
| `asset` | 资源适配器 |
| `scene` | 场景适配器 |
| `log()` | 输出日志 |
| `warn()` | 输出警告 |
| `error()` | 输出错误 |
| `openPanel()` | 打开面板 |
| `closePanel()` | 关闭面板 |

### 工厂函数

```typescript
// 获取适配器实例
getEditor(): IEditorAdapter
getPanel(): IPanelAdapter
getIPC(): IIPCAdapter
getAsset(): IAssetAdapter
getScene(): ISceneAdapter

// 版本检测
isV2(): boolean
isV3(): boolean
detectVersion(): CocosVersion
```

---

## 🎯 开发计划

### 进行中

- 🔄 Vue 数据编辑器面板开发
- 🔄 表格数据导入/导出功能
- 🔄 数据验证与校验系统

### 待开发

- 📋 Excel 文件导入支持
- 📋 JSON/CSV 数据格式支持
- 📋 数据模板系统
- 📋 批量编辑功能
- 📋 数据关联与引用检查

---

## 📝 版本历史

### v1.0.0 (2026-01-27)

- ✅ 搭建跨版本插件基础框架
- ✅ 实现版本检测器与适配器工厂
- ✅ 完成 v2/v3 适配器层
- ✅ 添加通用工具类（Tools）
- ✅ 添加双向同步功能（BidirectionalSync）
- ✅ 完善项目文档

---

## 📄 许可证

MIT License

---

## 👥 贡献

欢迎提交 Issue 和 Pull Request！

---

## 📮 联系方式

- 作者：oldP
- 项目：Table Tool
- 邮箱：306964942@qq.com

---

## 🙏 致谢

感谢 Cocos Creator 社区的支持！
